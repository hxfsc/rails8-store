<div>
  <div>
    名称
  </div>

  <div>
    <%= @product.name %>
  </div>

  <div>
    <%= @product.description %>
  </div>

  <div>
    <%= image_tag @product.featured_image if @product.featured_image.attached? %>
  </div>

  <div>
    库存:
    <%= @product.inventory_count %>
  </div>

  <div class="flex gap-2">
    <%= link_to "返回列表", @product, class: "rounded-full p-2 px-3 bg-blue-500" %>

    <% if authenticated? %>
      <%= link_to "编辑",
      edit_store_product_path(@product),
      class: "rounded-full p-2 px-3 bg-green-500" %>
      <%= button_to "删除",
      [:store, @product],
      method: :delete,
      data: {
        turbo_confirm: "确认删除吗",
      },
      class: "rounded-full p-2 px-3 bg-red-500 text-white cursor-pointer" %>
    <% end %>
  </div>
</div>
